{
  "name": "finance-manager-worker",
  "private": true,
  "type": "module",
  "description": "Cloudflare Worker + D1 API scaffold for Finance Manager (preparation; not yet live).",
  "license": "AGPL-3.0-only",
  "scripts": {
    "dev": "wrangler dev",
    "deploy:dev": "wrangler deploy --env dev",
    "deploy:prod": "wrangler deploy --env prod",
    "d1:create:dev": "wrangler d1 create finance-manager-dev",
    "d1:create:prod": "wrangler d1 create finance-manager",
    "d1:migrate:local": "wrangler d1 migrations apply finance-manager --local",
    "d1:migrate:dev": "wrangler d1 migrations apply finance-manager-dev --remote --env dev",
    "d1:migrate:prod": "wrangler d1 migrations apply finance-manager --remote --env prod",
    "d1:exec:local": "wrangler d1 execute finance-manager --local --command",
    "r2:create:dev": "wrangler r2 bucket create finance-manager-receipts-dev",
    "r2:create:prod": "wrangler r2 bucket create finance-manager-receipts",
    "secret:dev": "wrangler secret put JWT_SECRET --env dev && wrangler secret put GOOGLE_CLIENT_SECRET --env dev",
    "secret:prod": "wrangler secret put JWT_SECRET --env prod && wrangler secret put GOOGLE_CLIENT_SECRET --env prod",
    "typecheck": "tsc --noEmit",
    "cf-typegen": "wrangler types",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "hono": "^4.6.0",
    "pdf-lib": "^1.17.1",
    "xlsx": "file:../vendor/xlsx-0.20.3.tgz"
  },
  "devDependencies": {
    "@cloudflare/vitest-pool-workers": "0.17.0",
    "@cloudflare/workers-types": "^4.20241127.0",
    "typescript": "^5.7.2",
    "vitest": "4.1.9",
    "wrangler": "^4.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "workerd",
      "@cloudflare/workerd-linux-64",
      "esbuild"
    ]
  }
}
